[alias]
real-tests = "test --release --features local,vulkan --test functional_discovery real_pipeline"
tough-tests = "test --release --features local,vulkan,tough-tests --test tough_retrieval --test tough_relations --test tough_vision -- --test-threads=1 --nocapture"
